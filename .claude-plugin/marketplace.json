{
  "_comment": "Deprecated. opencode-blog is distributed via the OpenCode-compatible install scripts (install.sh / install.ps1) and the global ~/.config/opencode/ directories. Kept as a stub for backward compatibility."
}
