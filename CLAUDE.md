# Moved

Project instructions for this repo now live in [AGENTS.md](./AGENTS.md), the
file OpenCode reads by default. This stub remains only because OpenCode falls
back to `CLAUDE.md` for users still on the legacy filename.

If you are reading this in a Claude Code session, the rules in `AGENTS.md`
apply equally — open it.
